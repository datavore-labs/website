extends ../layout

block vars
	- var title='Datavore\256 Licenses'
	- var bodyClass='licenses'

block content
	section#licenses.container
		h1 License Information

		p Datavore<sup>&reg;</sup> uses the following third-party packages, licensed as listed below.

		h2 Datavore<sup>&reg;</sup> Core Applcation

		.license-container
			table
				thead
					tr
						th(scope="col") Package Name
						th(scope="col") Version
						th(scope="col") License
				tbody
					include core.tpl.jade

		h2 Datavore<sup>&reg;</sup> LDAP

		.license-container
			table
				thead
					tr
						th(scope="col") Package Name
						th(scope="col") Version
						th(scope="col") License
				tbody
					include ldap.tpl.jade

		h2 Datavore<sup>&reg;</sup> Basic Authentication

		.license-container
			table
				thead
					tr
						th(scope="col") Package Name
						th(scope="col") Version
						th(scope="col") License
				tbody
					include basic-auth.tpl.jade

